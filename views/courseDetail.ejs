<!DOCTYPE html>
<html>
<head>
  <title><%= course.courseName %> - Details</title>
</head>
<body>
  <h1><%= course.courseName %> - Details</h1>
  <p>Total Classes: <%= course.totalClasses %></p>
  <p>Classes Attended: <%= course.attendedClasses %></p>
  <p>Attendance Percentage: <%= ((course.attendedClasses / course.totalClasses) * 100).toFixed(2) %>%</p>
  
  <!-- Mark attendance form -->
  <h3>Mark Attendance for a New Session</h3>
  <form action="/courses/<%= course._id %>/mark" method="POST">
    <label>Did you attend this class?</label>
    <select name="attended" required>
      <option value="yes">Yes</option>
      <option value="no">No</option>
    </select>
    <button type="submit">Submit</button>
  </form>
  
  <!-- Link to edit the course -->
  <p><a href="/courses/<%= course._id %>/edit">Edit Course</a></p>
  
  <!-- Delete course form -->
  <form action="/courses/<%= course._id %>/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete this course?');">
    <button type="submit">Delete Course</button>
  </form>
  
  <!-- Back to dashboard -->
  <p><a href="/dashboard">Back to Dashboard</a></p>
</body>
</html>