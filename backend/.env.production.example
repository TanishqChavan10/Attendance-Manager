# Backend Production Environment Variables
# Copy this file and customize for your production deployment

# ============================================
# REQUIRED VARIABLES
# ============================================

# MongoDB Connection (REQUIRED)
# Get this from MongoDB Atlas: Database > Connect > Connect your application
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/attendance-manager?retryWrites=true&w=majority

# Session Secret (REQUIRED)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=your-generated-secret-minimum-32-characters

# ============================================
# SERVER CONFIGURATION
# ============================================

# Environment Mode (REQUIRED for production)
NODE_ENV=production

# Server Port (set by hosting platform or use default)
PORT=5001

# Frontend URL for CORS (REQUIRED)
# Must match your deployed frontend domain exactly
FRONTEND_URL=https://your-app.vercel.app

# ============================================
# PUSH NOTIFICATIONS (OPTIONAL)
# ============================================

# Disable for v1.0 production (recommended)
ENABLE_REMINDERS=false

# Only set these if ENABLE_REMINDERS=true
# Generate with: npx web-push generate-vapid-keys
#VAPID_PUBLIC_KEY=your-public-key
#VAPID_PRIVATE_KEY=your-private-key
#VAPID_SUBJECT=mailto:your-email@example.com
#REMINDER_HOUR=19
